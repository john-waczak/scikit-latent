def add(a: int, b: int) -> int:
    """
    Test function to add two integers.

    Parameters
    ----------
    a : int
        The first summand.
    b : int
        The second summand.

    Returns
    -------
    int
        The sum of `a` and `b`.
    """
    return a + b
